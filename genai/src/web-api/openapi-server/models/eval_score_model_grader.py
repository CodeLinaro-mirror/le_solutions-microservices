# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# coding: utf-8

"""
    IOT Solutions - Gen-AI Microservice APIs

    These REST APIs provide the capability to run inference on Qualcomm 9100 devices.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json




from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from openapi_server.models.eval_item import EvalItem
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class EvalScoreModelGrader(BaseModel):
    """
    A ScoreModelGrader object that uses a model to assign a score to the input.
    """ # noqa: E501
    type: StrictStr = Field(description="The object type, which is always `score_model`.")
    name: StrictStr = Field(description="The name of the grader.")
    model: StrictStr = Field(description="The model to use for the evaluation.")
    sampling_params: Optional[Dict[str, Any]] = Field(default=None, description="The sampling parameters for the model.")
    input: List[EvalItem] = Field(description="The input text. This may include template strings.")
    pass_threshold: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="The threshold for the score.")
    range: Optional[List[Union[StrictFloat, StrictInt]]] = Field(default=None, description="The range of the score. Defaults to `[0, 1]`.")
    __properties: ClassVar[List[str]] = ["type", "name", "model", "sampling_params", "input", "pass_threshold", "range"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in ('score_model',):
            raise ValueError("must be one of enum values ('score_model')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of EvalScoreModelGrader from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in input (list)
        _items = []
        if self.input:
            for _item in self.input:
                if _item:
                    _items.append(_item.to_dict())
            _dict['input'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of EvalScoreModelGrader from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "name": obj.get("name"),
            "model": obj.get("model"),
            "sampling_params": obj.get("sampling_params"),
            "input": [EvalItem.from_dict(_item) for _item in obj.get("input")] if obj.get("input") is not None else None,
            "pass_threshold": obj.get("pass_threshold"),
            "range": obj.get("range")
        })
        return _obj


