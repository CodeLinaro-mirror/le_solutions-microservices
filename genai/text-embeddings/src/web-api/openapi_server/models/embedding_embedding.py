# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# coding: utf-8

"""
    Embeddings, Models, Health & Ping API

    OpenAI-compliant API for creating embeddings and listing models, with health and ping endpoints.

    The version of the OpenAPI document: 1.0.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from typing import Any, List, Union, Dict
from pydantic import BaseModel, ConfigDict, field_validator
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self


class EmbeddingEmbedding(BaseModel):
    """
    The embedding vector. When encoding_format=base64, this will be a base64 string.
    """
    actual_instance: Union[List[float], str]

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
        arbitrary_types_allowed=True,
    )

    def __init__(self, actual_instance: Union[List[float], str], **kwargs) -> None:
        super().__init__(actual_instance=actual_instance, **kwargs)

    @field_validator('actual_instance')
    def validate_embedding(cls, v):
        """Validate that embedding is a list of floats or a base64 string"""
        if isinstance(v, str):
            # Base64 encoded string
            return v
        elif isinstance(v, list):
            # List of floats
            if len(v) == 0:
                raise ValueError("Embedding vector cannot be empty")
            if not all(isinstance(item, (int, float)) for item in v):
                raise ValueError("Embedding must be a list of numbers")
            return v
        else:
            raise ValueError("Embedding must be a list of floats or a base64 string")

    @classmethod
    def from_dict(cls, obj: Union[List[float], str, Dict]) -> Self:
        """Create instance from dict or direct value"""
        if isinstance(obj, dict) and 'actual_instance' in obj:
            return cls(actual_instance=obj['actual_instance'])
        else:
            # Direct value (list or string)
            return cls(actual_instance=obj)

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        data = json.loads(json_str)
        return cls.from_dict(data)

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        return json.dumps(self.actual_instance)

    def to_dict(self) -> Union[List[float], str]:
        """Returns the dict representation of the actual instance"""
        return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.actual_instance)
