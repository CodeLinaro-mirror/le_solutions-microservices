# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# coding: utf-8

"""
    Embeddings, Models, Health & Ping API

    OpenAI-compliant API for creating embeddings and listing models, with health and ping endpoints.

    The version of the OpenAPI document: 1.0.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from typing import Any, List, Union, Dict
from pydantic import BaseModel, ConfigDict, field_validator
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self


class CreateEmbeddingsRequestInput(BaseModel):
    """
    Input to embed. Can be a single string, an array of strings, or an array of token arrays.
    To embed multiple inputs in a single request, pass an array of strings or array of token arrays.
    """
    actual_instance: Union[str, List[str], List[List[int]]]

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
        arbitrary_types_allowed=True,
    )

    def __init__(self, actual_instance: Union[str, List[str], List[List[int]]], **kwargs) -> None:
        super().__init__(actual_instance=actual_instance, **kwargs)

    @field_validator('actual_instance')
    def validate_input(cls, v):
        """Validate that input is string, list of strings, or list of token arrays"""
        if isinstance(v, str):
            return v
        elif isinstance(v, list):
            if len(v) == 0:
                raise ValueError("Input list cannot be empty")
            # Check if it's a list of strings
            if all(isinstance(item, str) for item in v):
                return v
            # Check if it's a list of lists of integers (token arrays)
            if all(isinstance(item, list) and all(isinstance(token, int) for token in item) for item in v):
                return v
            raise ValueError("Input must be a string, list of strings, or list of token arrays (list of lists of integers)")
        else:
            raise ValueError("Input must be a string, list of strings, or list of token arrays")

    @classmethod
    def from_dict(cls, obj: Union[str, List, Dict]) -> Self:
        """Create instance from dict or direct value"""
        if isinstance(obj, dict) and 'actual_instance' in obj:
            return cls(actual_instance=obj['actual_instance'])
        else:
            # Direct value (string or list)
            return cls(actual_instance=obj)

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        data = json.loads(json_str)
        return cls.from_dict(data)

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        return json.dumps(self.actual_instance)

    def to_dict(self) -> Union[str, List[str], List[List[int]]]:
        """Returns the dict representation of the actual instance"""
        return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.actual_instance)
