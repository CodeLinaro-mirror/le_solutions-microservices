
server {
    listen 80;
    listen [::]:80;

    server_name _;

    root /usr/share/nginx/html;
    index index.html index.nginx-debian.html;

    location / {
        try_files $uri $uri/ =404;
    }

    include solutions/*.conf;

}
