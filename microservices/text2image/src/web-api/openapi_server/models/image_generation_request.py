# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

# coding: utf-8

"""
    Image Generation and Model Listing API

    OpenAI-compliant API for generating images using Qualcomm 9100/9075 devices.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json




from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class ImageGenerationRequest(BaseModel):
    """
    Request schema for generating images from a text prompt.
    """ # noqa: E501
    prompt: StrictStr = Field(description="A text description of the desired image(s).")
    background: Optional[StrictStr] = Field(default='auto', description="Sets transparency for the background.")
    model: Optional[StrictStr] = Field(default='stable-diffusion-2.1', description="The model to use for image generation.")
    moderation: Optional[StrictStr] = Field(default='auto', description="Controls content-moderation level.")
    n: Optional[Annotated[int, Field(le=1, strict=True, ge=1)]] = Field(default=1, description="Number of images to generate.")
    output_compression: Optional[Annotated[int, Field(le=100, strict=True, ge=0)]] = Field(default=100, description="Compression level for generated images.")
    output_format: Optional[StrictStr] = Field(default='png', description="Format of the generated images.")
    partial_images: Optional[Annotated[int, Field(le=0, strict=True, ge=0)]] = Field(default=0, description="Number of partial images to generate for streaming.")
    quality: Optional[StrictStr] = Field(default='low', description="Quality of the generated image.")
    response_format: Optional[StrictStr] = Field(default='url', description="Format in which images are returned.")
    size: Optional[StrictStr] = Field(default='512 x 512', description="Size of the generated image.")
    stream: Optional[StrictBool] = Field(default=False, description="Enables streaming mode.")
    style: Optional[StrictStr] = Field(default='vivid', description="Style of the generated image.")
    user: Optional[StrictStr] = Field(default=None, description="Unique identifier for the end-user.")
    __properties: ClassVar[List[str]] = ["prompt", "background", "model", "moderation", "n", "output_compression", "output_format", "partial_images", "quality", "response_format", "size", "stream", "style", "user"]

    @field_validator('background')
    def background_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('transparent', 'opaque', 'auto',):
            raise ValueError("must be one of enum values ('transparent', 'opaque', 'auto')")
        return value

    @field_validator('moderation')
    def moderation_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('low', 'auto',):
            raise ValueError("must be one of enum values ('low', 'auto')")
        return value

    @field_validator('output_format')
    def output_format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('png', 'jpeg', 'webp',):
            raise ValueError("must be one of enum values ('png', 'jpeg', 'webp')")
        return value

    @field_validator('quality')
    def quality_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('high', 'medium', 'low',):
            raise ValueError("must be one of enum values ('high', 'medium', 'low')")
        return value

    @field_validator('response_format')
    def response_format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('url', 'b64_json',):
            raise ValueError("must be one of enum values ('url', 'b64_json')")
        return value

    @field_validator('size')
    def size_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('512 x 512',):
            raise ValueError("must be one of enum values ('512 x 512')")
        return value

    @field_validator('style')
    def style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('vivid', 'natural',):
            raise ValueError("must be one of enum values ('vivid', 'natural')")
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of ImageGenerationRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        # set to None if background (nullable) is None
        # and model_fields_set contains the field
        if self.background is None and "background" in self.model_fields_set:
            _dict['background'] = None

        # set to None if moderation (nullable) is None
        # and model_fields_set contains the field
        if self.moderation is None and "moderation" in self.model_fields_set:
            _dict['moderation'] = None

        # set to None if n (nullable) is None
        # and model_fields_set contains the field
        if self.n is None and "n" in self.model_fields_set:
            _dict['n'] = None

        # set to None if output_compression (nullable) is None
        # and model_fields_set contains the field
        if self.output_compression is None and "output_compression" in self.model_fields_set:
            _dict['output_compression'] = None

        # set to None if output_format (nullable) is None
        # and model_fields_set contains the field
        if self.output_format is None and "output_format" in self.model_fields_set:
            _dict['output_format'] = None

        # set to None if quality (nullable) is None
        # and model_fields_set contains the field
        if self.quality is None and "quality" in self.model_fields_set:
            _dict['quality'] = None

        # set to None if response_format (nullable) is None
        # and model_fields_set contains the field
        if self.response_format is None and "response_format" in self.model_fields_set:
            _dict['response_format'] = None

        # set to None if size (nullable) is None
        # and model_fields_set contains the field
        if self.size is None and "size" in self.model_fields_set:
            _dict['size'] = None

        # set to None if stream (nullable) is None
        # and model_fields_set contains the field
        if self.stream is None and "stream" in self.model_fields_set:
            _dict['stream'] = None

        # set to None if style (nullable) is None
        # and model_fields_set contains the field
        if self.style is None and "style" in self.model_fields_set:
            _dict['style'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of ImageGenerationRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "prompt": obj.get("prompt"),
            "background": obj.get("background") if obj.get("background") is not None else 'auto',
            "model": obj.get("model"),
            "moderation": obj.get("moderation") if obj.get("moderation") is not None else 'auto',
            "n": obj.get("n") if obj.get("n") is not None else 1,
            "output_compression": obj.get("output_compression") if obj.get("output_compression") is not None else 100,
            "output_format": obj.get("output_format") if obj.get("output_format") is not None else 'png',
            "partial_images": obj.get("partial_images") if obj.get("partial_images") is not None else 0,
            "quality": obj.get("quality") if obj.get("quality") is not None else 'auto',
            "response_format": obj.get("response_format") if obj.get("response_format") is not None else 'url',
            "size": obj.get("size") if obj.get("size") is not None else '512x512',
            "stream": obj.get("stream") if obj.get("stream") is not None else False,
            "style": obj.get("style") if obj.get("style") is not None else 'vivid',
            "user": obj.get("user")
        })
        return _obj
